# Include optional dependencies during installation
optional=true

# Legacy peer deps for compatibility
legacy-peer-deps=true

# Prefer offline packages when possible
prefer-offline=true

# Ensure proper resolution
registry=https://registry.npmjs.org/ 